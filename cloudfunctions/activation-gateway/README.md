# activation-gateway

One-time activation-code service for the Matcha Phone private beta.

Required runtime environment variables:

- `ACTIVATION_LICENSE_PRIVATE_KEY`: PKCS8 ES256 private key with newlines escaped as `\\n`.
- `ACTIVATION_PUBLIC_KEY_ID`: identifier generated by `npm run activation:keygen`.

The function never logs activation codes or authentication tokens.
